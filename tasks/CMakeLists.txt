# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(threed_odometryTaskLib)
ADD_LIBRARY(${THREED_ODOMETRY_TASKLIB_NAME} SHARED 
    ${THREED_ODOMETRY_TASKLIB_SOURCES})
add_dependencies(${THREED_ODOMETRY_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${THREED_ODOMETRY_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${THREED_ODOMETRY_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${THREED_ODOMETRY_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${THREED_ODOMETRY_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${THREED_ODOMETRY_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${THREED_ODOMETRY_TASKLIB_HEADERS}
    DESTINATION include/orocos/threed_odometry)

